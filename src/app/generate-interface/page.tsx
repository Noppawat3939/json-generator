import { LazyLoad } from "@/components";
import { InterfaceContainer } from "@/container";
import React from "react";

const CreateInterfacePage = () => {
  return (
    <LazyLoad>
      <InterfaceContainer />
    </LazyLoad>
  );
};

export default CreateInterfacePage;
